"""
Command-line interface entry point for the package.

This module should:
- Parse command-line arguments (argparse, click, typer, etc.)
- Dispatch commands to the appropriate processing functions
- Act as a thin wrapper around the core logic (no heavy processing here)

Typical responsibilities:
- Define commands (e.g. process, quicklook, mask, index)
- Handle user input paths (data/, logs/, etc.)
- Configure logging
"""